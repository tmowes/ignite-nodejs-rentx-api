import { Response, Request } from "express";
import { container } from 'tsyringe'

import { {{pascalCase name}}UseCase } from "./{{pascalCase name}}UseCase";

export class {{pascalCase name}}Controller {

async handle(request: Request, response: Response): Promise<Response> {
  try {
  const {{camelCase name}} = container.resolve({{pascalCase name}}UseCase)
  const new{{pascalCase entity}} = await {{camelCase name}}.execute({ email })
  return response.status(200).json(new{{pascalCase entity}})
  } catch (error) {
  return response.status(400).json({ error: error.message });
  }
  }
  }
